function managePdf(action, courseName) {
    if (action === "upload") {
      alert(`Upload PDF for ${courseName}`);
      // Add file upload functionality here
    } else if (action === "delete") {
      alert(`Delete PDF for ${courseName}`);
      // Add file delete functionality here
    }
  }
  